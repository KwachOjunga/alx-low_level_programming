#include "main.h"

/**
 *
 *The function checks if the character is in upper case
 *Return 1; if uppercase
 *
 *Return 0: if lowercase
 */

int isalpha(int b)
{
  if ((b >= 97 && b <= 122) || (b >= 65 && b <= 90))
    {
      Return (1);
    }
  else
    {
      Return (0);
    }
  _puchar('\n');
}  
